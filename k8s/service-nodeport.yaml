apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: doomed-apt
  labels:
    app: backend
spec:
  type: LoadBalancer
  selector:
    app: backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
